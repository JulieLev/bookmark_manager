<h1> Links </h1>
  <body>
    <ul id='links'>
      <% @links.each do |link| %>
        <li>
          Title: <%= link.title %>
          URL:   <%= link.url   %>
        </li>
      <% end %>
    </ul>

    <a  href="/links/new">New</a>
</body>
